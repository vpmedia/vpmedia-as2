import net.manaca.lang.BObject;
/**
 * DataTable中一行数据
 * @author Wersling
 * @version 1.0, 2005-11-29
 */
class net.manaca.data.components.DataRow// extends BObject
{
	
	public function DataRow (rootNode:XMLNode)
	{
		/*var nodes:Array = rootNode.childNodes;
		var node:XMLNode;
		for (var i:String in nodes)
		{
			node = nodes[i];
			this[node.nodeName] = node.firstChild.nodeValue;
		}*/
	}
}